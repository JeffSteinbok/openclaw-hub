{
  "id": "outlook-work-calendar",
  "name": "Outlook Work Calendar",
  "description": "Fetch upcoming events from the published Outlook work calendar. Uses the EWS JSON API — no authentication required.",
  "version": "1.0.1",
  "entry": "./dist/adapter.js",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "calendarUrl": {
        "type": "string",
        "description": "Published Outlook work calendar base URL"
      },
      "folderId": {
        "type": "string",
        "description": "EWS folder ID for the calendar"
      }
    }
  },
  "contracts": {
    "tools": [
      "outlook_work_calendar_fetch"
    ]
  },
  "activation": {
    "onStartup": true
  },
  "openclaw": {}
}
