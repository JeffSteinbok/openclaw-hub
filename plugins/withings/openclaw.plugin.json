{
  "id": "withings",
  "name": "Withings",
  "version": "1.0.0",
  "description": "Fetch health data from Withings devices (weight, body composition, heart rate, sleep, activity)",
  "entry": "dist/index.js",
  "tools": "src/tools.py",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "clientId": {
        "type": "string",
        "description": "Withings OAuth2 App Client ID"
      },
      "clientSecret": {
        "type": "string",
        "description": "Withings OAuth2 App Client Secret (use env var WITHINGS_CLIENT_SECRET)"
      },
      "redirectUri": {
        "type": "string",
        "description": "OAuth redirect URI registered with the Withings developer app"
      }
    }
  },
  "source": {
    "canonicalRepo": "JeffSteinbok/openclaw-hub",
    "canonicalPath": "plugins/withings"
  },
  "activation": {
    "onStartup": true
  }
}
